pip install --upgrade pip

# 核心数据处理库
pandas>=1.3.0  # 用于解析CSV文件（处理表格数据）
python-dateutil>=2.8.1  # 日期时间处理（pandas依赖，通常会自动安装）

# 类型提示支持（可选，提升代码健壮性）
typing_extensions>=4.0.0  # 若使用Python 3.8及以下，补充typing功能

# 开发辅助（可选）
pytest>=7.0.0  # 单元测试（如需测试代码）
